<%= form_with(model: multiple_pic, local: true) do |form| %>
  <% if multiple_pic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(multiple_pic.errors.count, "error") %> prohibited this multiple_pic from being saved:</h2>

      <ul>
      <% multiple_pic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :project_id %>
    <%= form.number_field :project_id, id: :multiple_pic_project_id %>
  </div>

  <div class="field">
    <%= form.label :activity_id %>
    <%= form.number_field :activity_id, id: :multiple_pic_activity_id %>
  </div>

  <div class="field">
    <%= form.label :productor_id %>
    <%= form.number_field :productor_id, id: :multiple_pic_productor_id %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :multiple_pic_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
